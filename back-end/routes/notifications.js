// routes/notifications.js
import express from "express";
const router = express.Router();

// Temporary placeholder route
router.get("/", (req, res) => {
  res.json({ message: "Notifications route connected successfully!" });
});

export default router;
